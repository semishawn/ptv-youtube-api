-- Schema to create table for recent uploads
CREATE TABLE recent_uploads (
	title TEXT NOT NULL,
	id TEXT NOT NULL
);

-- Schema to create table for Parkland Profiles playlist
CREATE TABLE profiles_playlist (
	title TEXT NOT NULL,
	id TEXT NOT NULL
);